name: sample-app
display_name: Sample App
description: A minimal test app for SelfStack
version: 0.1.0

runtime:
  type: docker-compose
  entry: docker-compose.yml

expose:
  port: 3000
  health: /api/health
